#include <stdio.h>

int main()
{
    int myage=20;
    int votingage=18;

    if(myage>=votingage){
        printf("Old enough to vote");
    }
    else{
        printf("Not old enough to vote!");
    }
    
  
    
return 0;
}